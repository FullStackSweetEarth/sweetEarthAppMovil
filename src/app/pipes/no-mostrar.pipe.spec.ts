import { NoMostrarPipe } from './no-mostrar.pipe';

describe('NoMostrarPipe', () => {
  it('create an instance', () => {
    const pipe = new NoMostrarPipe();
    expect(pipe).toBeTruthy();
  });
});
