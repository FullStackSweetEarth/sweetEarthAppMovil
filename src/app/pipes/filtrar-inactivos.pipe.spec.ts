import { FiltrarInactivosPipe } from './filtrar-inactivos.pipe';

describe('FiltrarInactivosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarInactivosPipe();
    expect(pipe).toBeTruthy();
  });
});
